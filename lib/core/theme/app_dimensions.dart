/// Centralized dimensions and spacing values.
class AppDimensions {
  AppDimensions._();

  // ── Text sizes ──
  static const double textHeader = 25.0;
  static const double textDescription = 13.0;
  static const double textBody = 14.0;
  static const double textSmall = 12.0;

  // ── Spacing ──
  static const double pageMargin = 30.0;
  static const double cardPadding = 15.0;

  // ── Border radius ──
  static const double borderRadius = 10.0;
  static const double borderRadiusLarge = 15.0;
  static const double borderRadiusXL = 20.0;
}
