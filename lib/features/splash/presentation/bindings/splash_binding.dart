import 'package:get/get.dart';

/// Splash binding — currently no dependencies needed.
class SplashBinding extends Bindings {
  @override
  void dependencies() {
    // No dependencies required for splash page
  }
}
