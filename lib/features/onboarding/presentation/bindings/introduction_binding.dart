import 'package:get/get.dart';

/// Introduction binding — currently no dependencies needed.
class IntroductionBinding extends Bindings {
  @override
  void dependencies() {
    // No dependencies required for introduction page
  }
}
